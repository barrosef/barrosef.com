# ~/.headroom/proxy.env — every proxy setting lives here.
# The service reads it; nothing is configured anywhere else.
HEADROOM_MODE=token
HEADROOM_PROTECT_TOOL_RESULTS=Read,Edit,Write
HEADROOM_TOOL_DESC_MAX_CHARS=200
HEADROOM_LOG_FILE=/home/you/.headroom/logs/requests.jsonl
HEADROOM_TELEMETRY=on
